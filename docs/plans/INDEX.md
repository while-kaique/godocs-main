# Planos — índice

> Índice dos planos de sessão (ADR-026, refinado pelo ADR-034). Cada plano vive em seu **arquivo próprio**
> `docs/plans/<slug>.md`; este índice é o mapa rasteável. O `docs/NEXT-SESSION.md` é o **ponteiro enxuto** que
> aponta o plano **ativo**. Planos paralelos (tópicos/branches distintos) coexistem aqui sem se sobrescrever.

## Como cultivar (instrucional — sem hook)
- O **`/ggsd:plan`** cria `docs/plans/<slug>.md` (`Status: rascunho → aprovado (quem, data)`), adiciona/atualiza
  a linha aqui e marca o **ativo** (◀), e atualiza o ponteiro no `NEXT-SESSION.md`.
- O **`/ggsd:code`** segue o ponteiro até o arquivo do plano e só coda se o `Status` lá for `✅ aprovado` (RF-03).
- O **`/ggsd:handoff`** marca o plano `executado`/arquiva, atualiza esta tabela e **move o ponteiro** para o
  próximo plano ativo (ou "nenhum — próximo é planejar X"). Nunca deixa um plano `aprovado` órfão.
- **Slug** em kebab-case, **sem prefixo de data** (o git guarda a data/histórico — RF-17). Ex.: `cadastro-contatos`.

## Planos
| Plano | Status | Resumo (1 linha) |
|---|---|---|
| [feature-projeto-vinculado](feature-projeto-vinculado.md) | 🟢 **implementado — na branch `feat/projeto-vinculado`, ATÉ STAGING (24/08)**: plano aprovado (8 questões respondidas pelo Luis), Partes A/B/C codadas, **1710 testes verdes** (+20), tsc só com os 5 erros pré-existentes; aguarda deploy staging + push + revisão do peer (SEM prod, SEM PR) | Projeto como FEATURE de outro: toggle "novo × feature" na Etapa 1 (3 fluxos) + autocomplete do pai (via `sheet_espelho`, endpoint `GET /api/projetos/buscar`), colunas "ID Pai"/"ID Feature" no Sheets + `projeto_pai_id`/`projeto_filhos_ids` no SQLite, prefixo `[feature de <pai>]` no nome, e **aprovação sequencial**: líder do filho aprova → só então abre a fila do líder do dono do PAI (coluna `estagio` em `projeto_aprovacoes`, mensagem própria via Gomoon). ⚠️ toca invariantes D1–D31 |
| [relatorio-especiais-pendentes](relatorio-especiais-pendentes.md) ◀ **ATIVO** | ✅ **executado — T1–T3 (13/08, `9d351c8`)**: aba **"Especiais Pendentes +15 dias"** gravada na planilha de PROD (65 especiais · 30 pendentes · **19 acima de 15 dias**, o mais antigo com **53 dias**). Script + config novos, nenhum arquivo existente tocado, nada de runtime | Fila de trabalho para a triagem aprovar os ESPECIAIS **do mais antigo para o mais novo** ("galera tá reclamando"). Especial é a única fila sem rede: não abre fila de líder (**D27**) e pula o memorial financeiro, então a validação humana da RPA é a **única porta** — um especial parado é um projeto que ninguém olhou, e nada o denunciava. Irmão do `relatorio-espera.ts` (mesma mecânica: aba dedicada, dry-run por default, `ESPECIAIS_WRITE=1` para escrever) |
| [detalhe-triagem-abre-instantaneo](detalhe-triagem-abre-instantaneo.md) | ✅ **executado E DEPLOYADO (13/08)** — staging `edf400b4` v156 (13:24 UTC) + **prod `674a3710` (13:29 UTC)**, ambos com `erros=0` no cron pós-deploy. ⚠️ A **T7 (validação de navegador)** foi **DISPENSADA pelo Luis** — ninguém confirmou de olho que a ficha abre sem spinner. Antes: executado — T1–T6 (13/08, `14d94e0` na `feat/espelho-e-perf-navegacao`): 1443 testes verdes (baseline 1428), `worker.js` rebuildado, `CLAUDE.md` gotcha **3b** + `SPEC_CORRECOES.md`. ⛔ **T7 (staging `edf400b4` → prod `674a3710`) ABERTA** · ⚠️ os **3 revisores NÃO rodaram** (instrução do usuário de não disparar subagentes) → o `/ggsd:ship` barra o envio | Abrir uma linha no `/dashboard` ainda espera ~1 s **mesmo com o espelho**: o clique dispara uma requisição inteira (**~750 ms de overhead FIXO do edge**, já medido) e o `getProjetoDashboard` faz **2 leituras SQLite em série** (`lerLinhaEspelho` → `getAdminStatusLogs`), sem prefetch e sem cache — reabrir a mesma ficha paga tudo de novo. Fatia: `Promise.all` no servidor + **prefetch por INTENÇÃO** no hover/foco da linha (150 ms, a régua do `defaultPreloadDelay`) com cache de **30 s** por id, erro nunca cacheado e invalidação ao gravar. ⚠️ Introduz I/O no hover **de propósito** — seguro porque o alvo é o espelho (SQLite), não o Sheets (cota compartilhada com prod) |
| [integrar-espelho-e-perf-navegacao](integrar-espelho-e-perf-navegacao.md) | ✅ **executado — EM PRODUÇÃO (13/08, prod `674a3710`, 11:40:17 UTC)** — T1–T8 feitas. `origin/main` reincorporado antes do build (**`fa5ef97`**, pegou o **revert do #255** que ia para prod por engano), **1428 testes verdes**, `worker.js` 1.010,7 kb. **T6 verde**: 18 corridas seguidas com `espelhados=`/`erros=0` (o timeout de DO era transitório). Cron da prod `0 * * * *` → **`*/5`**; cold start 11:45 `espelhados=382 erros=0`, 11:50 `espelhados=0` = regime, sem timeout com 626 linhas. ⛔ **T9 (push + PR) ABERTA — branch NUNCA pushada**, então o `main` não tem nem o espelho nem os 2 commits de perf do Kaique: deployar do `main` REGRIDE a prod. ⚠️ Os 3 revisores NÃO rodaram → `/ggsd:ship` vai barrar | Junta numa branch só o **espelho da planilha** ([sqlite-fonte-de-leitura](sqlite-fonte-de-leitura.md), executado mas **nunca pushado** — 7 commits locais) e a **perf de navegação do Kaique** (`origin/perf/navegacao-chunks-e-swr`), sobre o `origin/main` atual, e fecha a **T12** (staging → validação → prod). ⚠️ Os dois atacavam a MESMA dor e nenhum estava no `main`; como o `updateApp` substitui a app inteira, cada deploy apagava o outro (v141 ↔ v146). A sobreposição foi **medida**: só **1 linha** colide (a chamada dentro de `listarMeusProjetos`) — o cache de 60 s dele **continua útil** porque o espelho deixa `contarPendentes({sync:true})` lendo a planilha de verdade |
| [chat-uma-mensagem-por-decisao](chat-uma-mensagem-por-decisao.md) | ✅ **CONCLUÍDO — EM PRODUÇÃO (12/08, prod version 237, 14:32 UTC)** · executado — T1–T6 (12/08): **1258 testes verdes** (baseline 1242), `worker.js` rebuildado, docs (CLAUDE.md + spec **§12 gotcha 9**), commitado na branch `feat/chat-notifica-so-pre-aprovacao`. ✅ **Os 3 revisores de contexto fresco voltaram e NENHUM barra o envio** (conformidade `diverge-baixa` · qualidade `sugestoes` · reuso `duplicacao-intencional`); o achado convergente dos 2 primeiros (o ramo mudo) foi corrigido em `8181772`. O que foi feito: `execContando` + `decidirAprovacoesDoProjeto` devolvendo `number \| null`, predicado puro `deveNotificarDecisao` em `notificacao-chat.ts`, gatilho passando por ele, 4 testes, docs (D30) e rebuild do `worker.js`. Os achados **2–6** da revisão ficam FORA (declarados nas Fronteiras) | Fecha o achado nº 1 da revisão de 12/08: o gate de `decidirAprovacao` é **check-then-act** (SELECT da linha `pendente` → UPDATE), então duplo clique / retry / 2 líderes da mesma fila (**D4**) notificam o grupo do Chat **2×**. O `UPDATE` já serializa (`AND veredito = 'pendente'`) — falta o `rowsWritten` chegar ao gatilho. ⚠️ **"não sei" ≠ "zero"**: nenhum caminho de produção lê `rowsWritten` hoje e o `env.DB` do Godeploy nunca foi exercitado nisso — um `> 0` cru sobre `undefined` trocaria a duplicata por **silêncio permanente**, daí o `null` **notificar** (mesmo default invertido do D30) |
| [chat-notifica-so-pre-aprovacao](chat-notifica-so-pre-aprovacao.md) | ✅ **CONCLUÍDO — EM PRODUÇÃO (12/08)**: T1–T7 (código, 1258 testes verdes, `worker.js`, CLAUDE.md + spec **D30**) + **T8** = staging `edf400b4` version 141 (13:51 UTC, runtime validado) → **prod `674a3710` version 237 (14:32 UTC)**. ⛔ Falta só o **PR** (branch ainda local). ⚠️ Conteúdo da mensagem só se confere na 1ª pré-aprovação REAL em prod. ✅ Os 3 revisores de contexto fresco **voltaram e nenhum barra o envio** (conformidade `diverge-baixa` · qualidade `sugestoes` · reuso `possivel-duplicacao`); 4 achados já corrigidos (commit `d7447eb`), 6 em aberto — o nº 1 é a fatia ativa, os 2–6 seguem declarados | O alerta do grupo do Google Chat deixa de sair a cada submissão/edição e passa a sair na **pré-aprovação do líder**. Quem nunca terá parecer (especial, autor liderança, sem líder, TeamGuide fora) notifica na submissão **sinalizado** — silenciar sumiria com o projeto do grupo. `ajuste`/`reprovado` não notificam; a 2ª mensagem por submissão (`Análise Pendente`, do `syncUpdateToGoogle`) é **suprimida**; o alerta do **especial** fica enxuto |
| [dispensa-fila-lider-reprovado](dispensa-fila-lider-reprovado.md) | ✅ **concluído — EM PRODUÇÃO (07/08)**: T1–T8 (06/08, spec **D29** + `CLAUDE.md`) + **T9** no deploy CONJUNTO com a ferramenta editável — staging `edf400b4` 13:00 UTC → prod `674a3710` 13:47 UTC, **1161 testes verdes**, PR **#242**. ⚠️ Validação **estrutural** (o `E2E_COOKIE` expirou): build provado por `getApp`, runtime **não** — e a dispensa só se observa quando o analisador reprovar alguém (havia **0** `claro_nao` em prod). Ressalva dos revisores de contexto fresco: `diverge-baixa` + `sugestoes` (nada barrante). | Projeto reprovado por critério (`claro_nao`) **dispensa** as linhas pendentes da fila do líder (`veredito='dispensado'`, `decidido_por='sistema'`): sai do backlog das DMs, do relatório de espera e da tela `/aprovacoes`; a coluna grava **`Dispensado`**, nunca `Pré-reprovado`. O mesmo fall-through afetava **3 telas**, não 1 — as outras 2 só apareceram na varredura/revisão. Parecer humano vence a dispensa; `reabrirPreAprovacoes` reabre sem `forcar` |
| [pre-aprovacao-lider-fila-e-decisao](pre-aprovacao-lider-fila-e-decisao.md) | ✅ **executado e mergeado (PR #235, 06/08)** — F1 entregue: fila `/aprovacoes` (slider, D15), checklist do gestor (D13/D16), espelho nas 2 colunas do Sheets e o **acesso de leitura do líder (D28)**, que era a T3 e faltava — o líder levava **403** ao abrir "Ler a documentação completa" (reportado por um líder REAL). ⚠️ A **T7** (barra de decisão dentro de `/projeto/$id`) foi **substituída** pelo card auto-suficiente + parecer na própria fila (D13/D15), e o R$ para o líder ficou no **card**, não na tela read-only. ⛔ Falta **deploy** staging → prod | **F1** da pré-aprovação do líder: fila em rota própria `/aprovacoes` (⚠️ mudou a decisão da spec, que previa "5ª aba de Meus Projetos"), tabela **`projeto_aprovacoes`** como verdade (por versão, D10) e espelho **mão única** na coluna `Aprovação do Líder` (**fora de `SAFE_UPDATE_FIELDS`** — senão se "pré-aprova" digitando na planilha) |
| [fix-ferramenta-too-big-submissao](fix-ferramenta-too-big-submissao.md) | ✅ **T1+T2 mergeados (PR #237, 06/08)** — o bloqueio de staging descrito no plano deixou de existir: a `main` foi sincronizada com o que está no ar e as 3 frentes vão juntas no mesmo deploy. **T3** (cap no `AfetadosInput`) segue FORA. ⛔ Falta **deploy** staging → prod | Bug em PROD (caso Josiely, 10 tentativas): o input "Especifique a ferramenta" (`step1.tsx`) **não tinha `maxLength`** e o schema tem `ferramenta: max(200)` → `ZodError` **`too_big`** cru virando **HTTP 500** no toast, em inglês. Fatias: `maxLength` na UI + guard 400 amigável no `iniciarSubmissao`. ⚠️ Limite de payload do edge foi **DESCARTADO** (sondado até 20 MB). Achado colateral separado: **proxy de LLM em timeout de 25 s em TODA chamada** |
| [teamguide-lideranca-e-areas](teamguide-lideranca-e-areas.md) | ✅ **executado E EM PRODUÇÃO (06/08) — a feature inteira subiu no `674a3710`, com a D26: o aviso ao líder é IMEDIATO (POST ao Gomoon no fim de `submeterParaValidacao`, chave por PROJETO — a diária engoliria a 2ª submissão do dia), commits `ae1835b`+`6af2636`, 1111 testes; o snapshot diário segue codado mas SEM cron; `GOMOON_TOKEN` nos 2 apps; o anúncio global saiu em prod pelo João Victor. ⚠️ Falta exercitar o wiring do submit numa submissão REAL, e a branch não foi para o `main`** (2026-08-03, `c9991be` → `1296e12`/`e4780cb`/**`1d3aeb2`**) — 856 testes verdes, `worker.js` rebuildado, spec com **D11** (liderança isenta) + **D12** (liderança grava `Pré-aprovado (liderança)`) + **D13** (card auto-suficiente + checklist de 3 perguntas + nomenclatura pré-aprovação); staging redeployada **16:26** com a D13 e a **DM real ligada** (DM chegou ao Lucas). **Falta só o Luis olhar a tela nova (`/aprovacoes?como=lucas.queiroz@gocase.com`) → prod → PR** e a coluna `Aprovação do Líder` no cabeçalho do Sheets (P2, Luis) | **F0** da pré-aprovação do líder (spec: `spec-docs/SPEC_APROVACAO_LIDER.md`): `buildLiderancaIndex()`/`getLideresDe()` derivando líder↔liderado de `/teams` + membros (os endpoints de liderança da TeamGuide dão **403**) + os 2 bugs achados no caminho — **paginação morta** (`?page=` é ignorado; o real é `pageNumber`/`pageSize`, teto 100 → hoje lê só 25) e o **"ÁREA NÃO IDENTIFICADA" que atinge 10 pessoas** (nós de diretoria/passthrough → fallback p/ o nome do próprio nó) — + `deriveAreaFromEmail` sobre `/employees/emails/{email}`. ⚠️ Toca o **caminho quente da submissão**; UI/tabela/DM ficam FORA (F1/F2) |
| [motivo-reenvio-traco-padrao](motivo-reenvio-traco-padrao.md) | ✅ **concluído** (2026-08-03) — T1–T5: staging `edf400b4` → **PR #221 mergeado** (`main` `c65e5a1`) → **prod `674a3710`**; entregue junto dos docs da pré-aprovação do líder (o deploy substitui a app inteira). ⚠️ Sem backfill dos legados em branco | Coluna "Motivo Reenvio" nascia em BRANCO (excluída do payload do append) em vez de seguir o padrão "texto vazio → —": append inicializa com "—" (nunca no update, para não apagar a triagem), admin que limpa o motivo grava "—" e o e-mail de reenvio para de exibir "Motivo: —" |
| [taxonomia-destino-ganho-e-anti-loop](taxonomia-destino-ganho-e-anti-loop.md) | ✅ **concluído** (2026-07-30) — T1–T7: staging `edf400b4` validada com o cenário-âncora ponta a ponta (agente pergunta **1×**, "3 vagas não repostas / time menor" **aceita de primeira**, seção gravada, AK preenchida, 160h/R$2.230,40), prod `674a3710` deployado, **PR #217 mergeado**. ⚠️ Achado novo: com contexto suficiente o agente **auto-preenche** a Seção 2.4 **sem perguntar** e inventa o destino (atalho heurístico do gate libera) → fatia própria | Fatia **A1** da frente das perguntas: constante única `TAXONOMIA_DESTINO_GANHO` (5 destinos, incl. **menos custo**) consumida pelos 3 textos que hoje exigem _"nomeado **E** entregar A MAIS"_ — daí as 5 reperguntas na redução de 3 auxiliares — + **anti-loop** no juiz do preview (bloco sai quando `alocacao_ganhos` já é `'ok'`/`'reperguntado'`). ⚠️ **jornada preguiçosa FORA** (decisão do Luis); `respostaAlocacaoVaga` **não se mexe** (o defeito é de prompt) |
| [remover-arquivo-e-doc-background](remover-arquivo-e-doc-background.md) | ✅ concluído — mergeado (PR #211) + prod (2026-07-23) | Etapa 2 do /submeter: remover de verdade arquivo já enviado (F1) + processar doc em background ao subir arquivos (F2) + ajuste "adiantar o background" |
| [edicao-etapa1-participantes](edicao-etapa1-participantes.md) | executado (2026-07-17) · **emendado 07/08 e EM PRODUÇÃO** (deploy conjunto, PR #242): a **ferramenta** passou a ser editável na Etapa 1 — o card "somente leitura" ficou só com **Escopo + Status**, o que **revoga em parte o R2**. ⛔ Falta o Luis validar no navegador (trocar a ferramenta, reenviar, conferir a coluna "Ferramenta"; testar legado + escopo externo) | Etapa 1 (participantes + papéis) editável na edição — T1–T3 + R1/R2 feitos+staging, T4 limitação, T5 validação/prod pendente |
| [ocultar-valor-meus-projetos](ocultar-valor-meus-projetos.md) | executado (2026-07-17) | Tirar o badge de valor R$ dos cards de "Meus Projetos" — esconder p/ todos + não serializar (INV-02); T1–T3 codados, falta T4 staging→prod |
| [calibragem-regua-criterio-e-resync-append](calibragem-regua-criterio-e-resync-append.md) | ✅ **concluído** (2026-07-30) — T6 validado na staging (a régua REPROVA a nuvem de palavras + guarda de falso-positivo passou), prod `674a3710` deployado, **PR #216 mergeado** | Calibrar a régua do `claro_nao` (o caso-âncora da nuvem de palavras saiu **zona cinzenta** na staging: o analisador aceitou o **entregável** como rastreabilidade e o "use com PARCIMÔNIA" absorveu o par recorrência+contrafactual falhando) + fechar o gap do **`resyncGoogle`** (linha ausente na planilha → no-op silencioso com `ok:true`; passa a cair para **append**) — as 2 pendências antes de `staging/criterios-coautor` ir a prod |
| [criterios-projeto-classificacao](criterios-projeto-classificacao.md) | ✅ **concluído** (2026-07-30) — T1–T8 + calibragem **em produção** (PR #216 mergeado, `main` `39deaf9`); pendência **humana**: calibrar a régua com o Rafa | Critério de projeto (nota do Rafa): 2 perguntas-chave na Etapa 2 (moveu o ponteiro? + onde verificar · se desligar hoje quem reclama) + "que processo mudou e quanto" no agente; analisador classifica **claro sim / claro não / zona cinzenta** com justificativa SEMPRE; `claro não` → **Reprovado** na planilha + `Motivo Reprovado`; colunas novas `Classificação`/`Motivo Reprovado`/`Motivo Reenvio`; **formulário continua não barrando** |
| [loadings-dashboard-admin](loadings-dashboard-admin.md) | ✅ **concluído** (2026-07-28) — PR #215 mergeado, staging+prod | Tirar a espera percebida do `/dashboard`: stale-while-revalidate no servidor, cache de auth em `sessionStorage`, leitura da planilha em paralelo com o auth e skeleton no lugar do spinner (sem cache em SQLite — planilha segue fonte única) |
| [dashboard-admin-sheets](dashboard-admin-sheets.md) | ✅ executado (2026-07-28) — staging+prod deployados; **PR ainda não aberto** (bloqueio local do `gh pr create`) | Dashboard do admin vira triagem sobre a **planilha** (lia SQLite → mostrava rascunho e status errado): busca/filas/paginação, ficha com todas as colunas e mudança de status gravando no Sheets + auditoria `admin_status_log` |
| [perguntas-agente-recorrencia-evidencia](perguntas-agente-recorrencia-evidencia.md) | 🟡 **parcial** (2026-07-30) — **T1** ✅ (baseline) · **T2** ✅ (absorvido pelo plano do critério, PR #216 em prod) · **T3/T4 ABERTOS**. Próxima fatia com escopo **fechado** e pendente de `/ggsd:plan`: **A1** (taxonomia de destino do ganho — aceitar "menos custo" — + anti-loop no juiz do preview) + **jornada preguiçosa** (só perguntar com linha ≥176h); split carga×escala **não se mexe** (decisão do Luis) | Melhorar as perguntas dos agentes **e o fluxo de coleta** (onde cada informação é colhida: form × conversa × já sabido) embutindo recorrência/contrafactual/rastreabilidade (nota do Rafa); **barrar submissão está FORA em definitivo**; baseline medido em 24 conversas reais → `docs/analise-perguntas-agente.md` |
| [aceitar-zip-submissao](aceitar-zip-submissao.md) | ✅ executado (2026-07-22) | Aceitar upload de .zip na Etapa 2 — descompactar no cliente (fflate) e reusar o pipeline de addFiles (node_modules/whitelist/tokens); caso Rafael Lobo |
